use super::ast;
use super::token::Token;

/// Use me to make sure the syntax is good and produce a `Program`!
pub fn parse(_source: Vec<Token>) -> Result<ast::Program, idioma::Error> {
    todo!("sharpvik")
}
